@using com.SML.Lib.Common
@using com.SML.BIGTRONS.Enum
@using com.SML.BIGTRONS.ViewModels
@model TCMembersVM
@{

    var X = Html.X();
    var iWindowWidth = 650;    
}
@(
    X.Window()
   .CloseAction(CloseAction.Destroy)
   .ID("CalendarPrev")
   .Modal(true)
   .Title("Event")
   .Width(650)
   .Html((string)ViewData["content"])
            //Html.Raw(ViewData["content"])
            

)
